apiVersion: v1
kind: ServiceAccount
metadata:
  name: orchestrator
  namespace: orchestrator
  annotations:
    # ARN of irsa-roles.yaml's OrchestratorRole output (OrchestratorRoleArn).
    eks.amazonaws.com/role-arn: __ORCHESTRATOR_IRSA_ROLE_ARN__
